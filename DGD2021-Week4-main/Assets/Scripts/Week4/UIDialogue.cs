using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class UIDialogue : MonoBehaviour
{

    /*
     * both variables below MUST BE SET AT THE INSPECTOR (drag & drop)
     * 
     * */

    public Text currentText;    //the UI object that will display the text to the player
    public Image speakerImg;    //the Image that will display the image file to the player

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
